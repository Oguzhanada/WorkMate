Hobby mode: hourly cron is disabled in `vercel.json`.

When you upgrade to Vercel Pro, restore `vercel.json` from `vercel.pro.json.example`.

If needed, the Hobby daily SLA cron can be removed entirely instead of keeping it.
